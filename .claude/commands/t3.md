---
description: Start work in the T3 lane (prescription delivery + pharmacy stock) — loads context, picks the next task
argument-hint: [task number, or a description, or nothing for "whatever is next"]
---

You are working in **lane T3 — prescription delivery and pharmacy**.
Language: TypeScript. Directories: `apps/web/app/(patient)/prescription/`,
`apps/web/app/(pharmacy)/`, `apps/web/app/api/pharmacies/`.

## 1. Load your context, in this order

Read these now, before doing anything else:

1. @agents/status/T3.md — where the lane left off. Always first.
2. @agents/lanes/T3-pharmacy.md — scope, boundaries, and the build-order table that maps
   every task to its architecture section and its files.
3. @db/002_seed.sql — the pharmacies and stock you are building against, including the
   deliberate out-of-stock gap.
4. @PROGRESS.md — what the other lanes changed while you were away, and who is blocked.

Then read `Docs/Project_Vaidhya_Technical_Architecture_v1.md` §11 and §11.1 — both are
short, read both. Also read the `pharmacies`, `stock_items`, `prescriptions`,
`pharmacy_queue` and `bills` tables in `db/001_schema.sql`, and the top comment of
`apps/web/app/globals.css` for the frozen theme rules.

## 2. Pick the task

$ARGUMENTS

If that is empty, take the first `todo` row in the build-order table. Task 2 (the nearby
endpoint) has no dependency on anyone, so it is the right place to start if nothing is.

State which task you picked and which architecture sections you read, then build it.

## 3. Rules you do not break

- **Agree directory ownership with T2 before adding files under `apps/web/app/`.** The
  standing proposal, in T2's status file: you take `(patient)/prescription/**`,
  `(pharmacy)/**` and `api/pharmacies/**`; T2 takes the rest. Confirm before you build.
- **The patient portal *is* the delivery channel.** Nothing about the prescription
  depends on SMS. SMS is a reminder *about* it, and it is phase 2 and T4's.
- **One pharmacy is deliberately out of stock** on a prescribed medicine (Amala
  Medicals, Paracetamol 500mg). That gap is the entire point of the screen. Do not
  quietly fix the data.
- `stock_items.status` is a **generated column** — derived from quantity in the DB.
  Never write it by hand.
- The theme is frozen: light mode only, flat 2D, green for primary actions, amber and
  red only for urgency.
- If T2's prescription form does not exist yet, insert a `prescriptions` row by hand and
  build against that rather than waiting.

## 4. Before you finish

Update @agents/status/T3.md — and only that file:

- move what now works into **Done**, described as behaviour, with how you verified it
- set **In progress** to what you are mid-way through, or say nothing
- keep **Next** to the next two or three tasks, in order
- record anything blocking you under **Blocked**, naming the lane
- put anything that changes how another lane writes code under **Notes for other lanes**
- update the frontmatter: `state`, `now`, `demo_steps_done`, `updated`

Only add a number to `demo_steps_done` if you could perform that step live, right now, in
front of a judge.
