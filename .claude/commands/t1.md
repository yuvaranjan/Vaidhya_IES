---
description: Start work in the T1 edge-AI lane (Python/FastAPI voicebot) — loads context, picks the next task
argument-hint: [task number, or a description, or nothing for "whatever is next"]
---

You are working in **lane T1 — the edge-AI service**, owned by Yuvaranjan.
Language: Python only. Directory: `services/edge-ai/`.

## 1. Load your context, in this order

Read these now, before doing anything else:

1. @agents/status/T1.md — where the lane left off. Always first.
2. @agents/lanes/T1-edge-ai.md — scope, boundaries, and the build-order table that maps
   every task to its architecture section and its files.
3. @services/edge-ai/contracts.py — the frozen contract you must satisfy exactly.
4. @PROGRESS.md — what the other lanes changed while you were away, and who is blocked.

Then, for the specific task you are about to do, read **only** the architecture sections
the build-order table names for it, from
`Docs/Project_Vaidhya_Technical_Architecture_v1.md`. Grep for the section heading and read
that range — never read the whole document, it is 800 lines.

If your task is one of the fiddly ones, also read the corresponding part of
`Docs/Project_Vaidhya_V1_Build_Plan.md` §4, which has the estimates and the definition
of done.

## 2. Pick the task

$ARGUMENTS

If that is empty, take the first `todo` row in the build-order table that is not blocked,
preferring whichever one moves a §17 demo step (see `agents/demo-steps.json`). Tasks 2, 3
and 4 (the providers) unblock task 7, which is the core of the lane — so if nothing is
started, start there.

State which task you picked and which architecture sections you read, then build it.

## 3. Rules you do not break

- **Stay in `services/edge-ai/`.** The one exception is
  `apps/web/app/api/specialist/route.ts`, which is yours. Everything else under
  `apps/web/` belongs to T2 — do not edit it, not even a typo. Note it in your status
  file instead.
- **No `asyncio` timers, no background tasks.** Pending-finding state is computed from
  timestamps on every read.
- **JSON parse failure retries once, then falls back to a fixed safe question.** Never
  let a bad parse crash a turn.
- **Every response carries the English text.** Always.
- Changing `contracts.py` means changing `packages/shared/http.ts` and `mqtt.ts` in the
  same five minutes, and telling T2.
- Test with `services/edge-ai/requests.http` and curl. **Do not open a browser** — if you
  are debugging in a browser you are in T2's lane.

## 4. Before you finish

Update @agents/status/T1.md — and only that file:

- move what now works into **Done**, described as behaviour, with how you verified it
- set **In progress** to what you are mid-way through, or say nothing
- keep **Next** to the next two or three tasks, in order
- record anything blocking you under **Blocked**, naming the lane
- put anything that changes how another lane writes code under **Notes for other lanes**
- update the frontmatter: `state`, `now`, `demo_steps_done`, `updated`

Only add a number to `demo_steps_done` if you could perform that step live, right now, in
front of a judge.
