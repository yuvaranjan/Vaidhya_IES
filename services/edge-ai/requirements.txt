# Project Vaidhya — edge-ai (T1 lane). Python only. Runs on Node A (the edge laptop).
#
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   pip install -r requirements.txt
#   uvicorn main:app --reload --port 8000

fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12        # /voice/turn is multipart
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

httpx>=0.27                     # LM Studio (OpenAI-compatible) + everything else
groq>=0.11                      # Whisper turbo STT + LLM fallback + specialist
edge-tts>=6.1                   # Malayalam/Tamil/Hindi TTS, no API key, works offline-ish
paho-mqtt>=2.1                  # Seam B
supabase>=2.9                   # report write

# Optional — local translation. Heavy; the cache + LLM path works without it.
# transformers>=4.44
# torch
